import { createGlobalStyle } from 'styled-components';
/**
 * This file will contain our global styles. Global styles are styles that
 * are used by all elements in a page.
 */

export default createGlobalStyle`
 *{
     margin: 0;
     padding: 0;
     outline:0;
     box-sizing:border-box;
     font-family: 'Open Sans', sans-serif; 
 }
 #root{
     margin:0 auto;
 }
`